/**
 * Assertion: Validation of Expected State.
 * expect
 * -- General Assertions
 *    : we are performing the validation on the end values (scalar property)
 *      examples :
 *           
 * -- Locator Assertions: these assertions are perform on the locator.
 *    : you create locator object then you check the state of that locator.
 * -- Page Assertions
 *    : Validation performed on the page
 *      await expect(page).toHaveTitle('')
 *      await expect(page).toHaveUrl('')
 * -- soft Asserts
 */