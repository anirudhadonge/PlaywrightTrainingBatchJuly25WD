/**
 * id
 * class
 * link
 * xpath
 *  1. syntax : //<html tag>[@<attribute name>='<attribute property>']
 *  2. Navigate from one element to the other
 *     a. Parent to child ://<html tag>[@<attribute name>='<attribute property>']//<html Tag of Child>[@<attribute name>='<attribute property>']
 *     b. child to parent: //<html tag>[@<attribute name>='<attribute property>']//parent::<html tab of Parent>[@<attribute name>='<attribute property>']
 *     c. child to ancestor: //<html tag>[@<attribute name>='<attribute property>']//ancestor::<html tab of ancestor>[@<attribute name>='<attribute property>']
 *     d. moving between siblings
 *         1. following-siblings: //<html tag>[@<attribute name>='<attribute property>']//following-sibling::<html tab of sibling>[@<attribute name>='<attribute property>']
 *         2. preceding-siblings: //<html tag>[@<attribute name>='<attribute property>']//preceding-sibling::<html tab of sibling>[@<attribute name>='<attribute property>']
 * 
 *  3.text()
 *  4.contains(): its can be used for text and attributes
 *  5.And and OR
 *  6. * represents any element tag on the html page.
 * 
 * 
 * css
 * 1.class attribute is represented by '.' syntax is .<class value>
 * 2. Id attribute is represented by '#' syntax is #<value of id>
 * 3. [<Attribute Name>='<Attribute value>']
 * 4. parent to child
 * 5. siblings : you can move to immediate sibling
 */

