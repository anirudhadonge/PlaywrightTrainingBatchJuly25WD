/**
 * Auto waits
 * wait
 * 
 * waitforTimeout() : It haults the execution for a time period. Thread.sleep()
 * waitfor(locator , options)
 * waitForLoadstate()
 *  -- load : Page and All resources loaded.(images, css styling, scripts);
 *  -- domcontentLoaded : DOM object ready
 *  -- networkidle : No more network class in progress.
 */